
/*const bookmarks = [];
const adding = false;
const error = null;
const filter = 0;
const newBookmark = false;
// this.store.bookmarks == this.bookmarks
function findById(id) {
    // find bookmark
    let foundBookmark = this.bookmarks
        .find((currentBookmark) => 
            currentBookmark.id === id
        );
	return foundBookmark;
}


function addBookmark(bookmark) {
    //this function is responsible for adding bookmark to list
	this.bookmarks.push(bookmark);
}


function findAndDelete(id) {
    //this function is responsible for matching bookmark id
    this.bookmarks = this.bookmarks
        .filter((currentBookmark) => 
            currentBookmark.id !== id);
}


function filterByRatings() {
    //this function is responsible for filtering bookmarks by rating
    let filteredList = this.bookmarks
        .filter((currentBookmark) => 
            currentBookmark.rating >= this.store.filter);
	return filteredList;
}

//set filter == setBookMarkFilter
//Defines store filter
function setBookMarkFilter(rating) {
	this.filter = rating;
}
export default {
    bookmarks,
    adding,
    error,
    filter,
    newBookmark,
	addBookmark,
	findById,
	findAndDelete,
	filterByRatings,
	setBookMarkFilter,
}
*/